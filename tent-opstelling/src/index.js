import React from "react";
import ReactDOM from "react-dom/client";
import TentLayoutTool from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <TentLayoutTool />
  </React.StrictMode>
);